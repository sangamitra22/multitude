# Screenshots

Drop the images referenced from the top-level `README.md` here:

- `wallet-receipt-exports.png` — `/wallet` transaction drawer after a Testnet deploy reaches **Finalized**. Include the Deploy receipt panel (hash, gas, timestamp, Explorer link) and the **Copy raw / Export raw JSON / Export decoded JSON** action row.
- `settings-contracts-validation.png` — `/settings/contracts` with a bad hash, a truncated public key, and a sub-`2500000000` motes value so the inline validation errors, char counters, and disabled **Save** button are all visible.

Optional extras: `/` hero, `/dashboard` per persona, each `/agents/*` workflow, `/architecture`.
